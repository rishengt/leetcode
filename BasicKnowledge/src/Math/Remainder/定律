Given 2 nums 'a' and 'b':
If a % k == x and b % k == k - x :
then (a + b) is divisible by k

Given a array [a1,a2,a3,...., aN]
if (a1 % k + a2 % k + ... aN % k) % k = 0
then sum(array) % k = 0

if a%k = b%k = c, then (a-b)%k = 0;
if a%k + b%k = k, then (a+b)%k = 0;

